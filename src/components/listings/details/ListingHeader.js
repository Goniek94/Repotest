/**
 * ListingHeader is now empty - all information is shown above technical details
 * @param {Object} props
 * @param {Object} props.listing - Listing object with all details.
 */
const ListingHeader = ({ listing }) => {
  return null; // Nie wyświetlaj nic nad zdjęciami
};

export default ListingHeader;
